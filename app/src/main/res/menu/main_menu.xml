<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/search"
        android:title="Поиск матча по команде"
        android:icon="@drawable/ic_search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/download_and_save_to_db"
        android:title="Загрузить и сохранить в БД"
        android:icon="@drawable/ic_refresh"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/clean_db"
        android:title="Очистить БД"
        android:icon="@drawable/ic_clean_db"
        app:showAsAction="ifRoom"/>

</menu>